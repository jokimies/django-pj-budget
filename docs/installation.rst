============
Installation
============

At the command line::

    $ easy_install django-pj-budget

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pj-budget
    $ pip install django-pj-budget
